import { Layout } from '../components/Layout'
import { CreateUser } from '../components/CreateUser'

const HooksMemo = (): JSX.Element => {
  return (
    <Layout title="Hooks Memo">
      <CreateUser />
    </Layout>
  )
}
export default HooksMemo
